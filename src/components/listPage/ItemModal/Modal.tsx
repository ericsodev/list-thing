import React from "react";
import { motion } from "framer-motion";
import { Item } from "@/graphql/types/graphql";
import { ListBySlugQuery } from "@/graphql/types/graphql";


export default function Modal() {
    return <motion.div></motion.div>;
}
